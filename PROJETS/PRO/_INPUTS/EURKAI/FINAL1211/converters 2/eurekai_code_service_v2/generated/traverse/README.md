# traverse

> Auto-generated EUREKAI Code Service module

**Source:** `traverse.js`  
**Generated:** 2025-12-13T07:20:19.080313  
**Score:** 99/100

## Quick Start

### 1. Start the Node.js server

```bash
cd traverse
npm install
npm start
```

Server runs at `http://localhost:3000`

### 2. Use from Python

```python
from traverse_client import *

# Optional: configure different server
configure("http://localhost:3000")

# Check connection
assert health_check(), "Server not running!"

# Populate store with data
Store.set("Object", {"name": "Object", "attributeBundle": {"owned": []}})
Store.set("Object:Entity", {"name": "Entity", "attributeBundle": {"owned": []}})

# Call functions
result = traverse("lineage", options)
result = resolve_secondary_method("lineage", "centralMethod")
result = collect_inherited_elements("lineage", "bundleType")
```

## Available Functions

### `traverse`

Traverse le lineage d'un objet du plus spécifique au plus générique (ou inverse)

**Parameters:**
- `lineage` (str): 
- `callback` (callable): 
- `options` (dict):  *(optional)*

**Variants (no callback needed):**
- `traverse_collect()` - Collect all traversed items
- `traverse_find(field, value)` - Find first match
- `traverse_exists(field, value)` - Check if match exists

### `resolveSecondaryMethod`

Résout une méthode secondaire pour un lineage donné

**Parameters:**
- `lineage` (str): 
- `centralMethod` (str): 


### `collectInheritedElements`

Collecte tous les éléments hérités pour un lineage

**Parameters:**
- `lineage` (str): 
- `bundleType` (str): 


### `getAncestors`

Trouve tous les ancêtres d'un lineage

**Parameters:**
- `lineage` (str): 


### `getDescendants`

Trouve tous les descendants d'un lineage

**Parameters:**
- `lineage` (str): 



## Store API

The server includes a key-value store accessible from Python:

```python
from traverse_client import Store

Store.set("key", {"any": "value"})
value = Store.get("key")
all_keys = Store.keys()
Store.clear()
```

REST endpoints:
- `POST /store/set` - `{"key": "...", "value": ...}`
- `GET /store/get/:key`
- `GET /store/keys`
- `POST /store/clear`
- `POST /store/bulk` - `{"items": [{"key": "...", "value": ...}, ...]}`

## API Reference

### Health Check
```
GET /health
→ {"status": "ok", "functions": [...]}
```

### List Functions
```
GET /functions
→ {"functionName": {"params": 2, "isVariant": false}, ...}
```

### Call Function
```
POST /call
{"fn": "functionName", "args": [arg1, arg2, ...]}
→ {"ok": true, "result": ...}
```

### Batch Call
```
POST /batch
{"calls": [{"fn": "func1", "args": [...]}, {"fn": "func2", "args": [...]}]}
→ {"ok": true, "results": [{"ok": true, "result": ...}, ...]}
```

## Dependencies

traverse requires:
- `Store` - Key-value store (mock provided, replace with real implementation)

---
*Generated by EUREKAI Code Service*