# Tests de départ — T1 / T2 / T3

- T1 : cockpit + JSON statique (`tests/data/objects_T1.json`)
- T2 : cockpit + backend mock (in-memory)
- T3 : scénario GEVR minimal de création d’enfants pour `Object`.
