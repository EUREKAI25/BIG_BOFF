# Cockpit Eurkai — WEB_PGCD

Place ici le fichier `object_fractal_tool_v4.html` (la dernière version générée).
Le cockpit suppose :

- un accès à un fichier JSON : `../tests/data/objects_T1.json`
- une future connexion à un backend mock (T2), puis réel (T3).

Pour le moment, le but est de tester :

1. l’affichage de l’arbre d’objets
2. la vue fractale
3. la navigation entre objets
